let (id)abc be xyz;
let (num)abcd be 213;
let (string)abcde be "hallo";
let (exit)myname be 222;
let (exit) myname2 (num) test (string) test2 be {

    hallo
};
let (exit) myname2 () be {

    hallo
};